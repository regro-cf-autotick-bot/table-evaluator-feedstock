{% set name = "table-evaluator" %}
{% set version = "1.7.2.post1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: 2b510f1487623c5723fa89f53fe1b86f24cd38649b25178cd3c22c0ae07e86c3

build:
  noarch: python
  script:
    - sed -i.bak 's/dython==0.7.3/dython/g' setup.py
    - {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  build:
    - sed     # [unix]
    - m2-sed  # [win]
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - pandas ==2.0.*
    - numpy
    - tqdm
    - psutil
    - dython >=0.7.3,<0.8.0
    - seaborn
    - matplotlib-base
    - scikit-learn
    - scipy <=1.11.4

test:
  imports:
    - table_evaluator
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Baukebrenninkmeijer/Table-Evaluator
  summary: A package to evaluate how close a synthetic data set is to real data.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - dillonroach
